# Use hoisted node_modules so platform-specific optional dependencies
# (e.g. @rollup/rollup-win32-x64-msvc) resolve correctly on all platforms.
# The isolated linker (pnpm default) has a known bug with optional deps:
# https://github.com/npm/cli/issues/4828
node-linker=hoisted
